machine:
  ghc:
    version: 7.10.1

dependencies:
  override:
    - cabal install --only-dependencies --enable-tests

test:
  pre:
    - cabal configure --enable-tests --enable-library-coverage && cabal build
  post:
    - cabal install hpc-coveralls
    - .cabal-sandbox/bin/hpc-coveralls hspec